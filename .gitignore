**/.DS_Store

# herdr runtime data (server writes into ~/.config/herdr = dot_config/herdr symlink)
dot_config/herdr/*.log
dot_config/herdr/*.sock
dot_config/herdr/session.json

# herdr claude integration installs/updates this hook (see header comment).
# Regenerated by herdr's integration setup; keep out of version control.
dot_claude/hooks/herdr-*.sh
